<tr>
  <td><%= team.name %></td>
  <%= render_numeric_cells team.scores[0..8] -%>
  <%= render_numeric_cell  Tally.safe_sum(team.scores[0..8]), class: 'calculated' %>
  <%= render_numeric_cells team.scores[9..17] -%>
  <%= render_numeric_cell  Tally.safe_sum(team.scores[9..17]), class: 'calculated' %>
  <%= render_numeric_cell  team.total_score, class: 'calculated' %>
</tr>
